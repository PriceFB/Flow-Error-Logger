# Flow-Error-Logger
Flow Error Logger is a drop-in Salesforce package that captures Flow fault-path errors into a structured, queryable custom object. Add the bulk-safe, non-throwing "Log Flow Error" invocable action to any fault connector and stop Flows failing silently. Includes a demo flow, tests (97% coverage), and a notifier extension point. MIT.

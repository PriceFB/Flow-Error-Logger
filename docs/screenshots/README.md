# Screenshots

Drop README images here and reference them from the top-level `README.md`.

Suggested captures:

- `flow-error-log-record.png` — a `Flow_Error_Log__c` record (e.g. FEL-00001) showing the captured fault message, severity, element name, running user, and context.
- `demo-flow.png` — the `Demo_Flow_Error_Logger` flow in Flow Builder, highlighting the fault connector from **Trigger Fault** to the **Log Flow Error** action.
- `flow-error-logs-tab.png` — the Flow Error Logs list view/tab.
